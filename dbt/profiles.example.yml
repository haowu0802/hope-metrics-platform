# Copy to: ~/.dbt/profiles.yml
# Fill in real values. Do NOT commit the filled file.

hope_metrics:
  target: dev
  outputs:
    dev:
      type: postgres
      host: YOUR_HOST
      user: YOUR_USER
      password: YOUR_PASSWORD
      port: 5432
      dbname: YOUR_DB
      schema: public
      threads: 2
      sslmode: require
