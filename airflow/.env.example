# Copy to airflow/.env and fill in.
AIRFLOW_UID=50000

# Local-only Fernet key (required). Generate with:
#   python3 -c "import base64,os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
AIRFLOW__CORE__FERNET_KEY=

# Optional Feishu/Lark bot webhook for DAG failures
FEISHU_WEBHOOK_URL=
